// final onExit = ReceivePort();
  // final isolate = await Isolate.spawn(
  //   (message) => startFunc(message),
  //   onExit.sendPort,
  // );
  // onExit.listen(
  //   (message) {
  //     print(message);
  //     isolate.kill();
  //     onExit.close();
  //   },
  // );
//   Future<void> startFunc(SendPort port) async {
//   port.send(true);
// }

